# Agent Packages

This directory hosts standalone agent packages. An agent package distributes a specialized worker recipe defining role instructions, tool permissions, budget, and result contracts.

Core and maintenance agents remain built into Clio Coder under `src/domains/agents/builtins/`.

Community agent package authoring templates and validator guidelines will be introduced in Slice 2 under `library/_authoring/templates/agent/`.
