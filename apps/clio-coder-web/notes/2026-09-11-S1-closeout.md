# S1 authorized closeout — 2026-09-11

**S1 is done.** The operator explicitly approved applying the prepared one-line
checker update as an S1 boundary exception, rerunning the checks, and closing S1
if root lint retained only the documented baseline failures. Those conditions
are satisfied. No later slice was started.

Start and end HEAD: `1e1162f687a610f19f27a34495ad06251b237260`, branch `v048`,
unchanged from both the reviewed baseline and the previous ledger row. Node
`v24.20.0`, pnpm `10.34.5`, Linux x64. The session rechecked the working-tree
state and the relevant local `checkCiScripts` expectation before applying the patch.

The only code change in this closeout is `scripts/check-hygiene.ts:441`: append
`&& pnpm run test:web` to the expected root `ci` script. This matches the
manifest change S1 already required. The checker remains enabled and still
requires the exact CI command. No other checker logic or root source changed.
The prepared [patch](S1-ci-hygiene.patch) is retained as historical evidence and
has now been applied; it is no longer a pending action.

| Verification rerun | Result |
| --- | --- |
| `pnpm install --frozen-lockfile` | PASS before editing; no dependency updates |
| Root `pnpm run build` | PASS |
| `pnpm --filter @iowarp/clio-coder-web build` | PASS |
| `pnpm --filter @iowarp/clio-coder-web verify` | PASS: both TypeScript programs, app Biome, all **19 tests**, and production client build |
| Root `pnpm run typecheck` | PASS |
| Root `pnpm run lint` | FAIL only on the two documented baseline docs-parity conditions; no new lint regressions |
| `git diff --check` | PASS |
| Boundary / scope audit | Only the approved one-line checker change was added outside the app in this closeout |

App tests reran authentication and Host/Origin handling, production worker
inventory/removal, fabricated installation and streamed progress, idempotency,
cancellation refusal, queue overflow, HTTP and RPC deadlines, worker restart,
SSE replay and bounds, OpenAPI validation/freshness, import boundaries, static
and install containment, and launcher shutdown. The browser screenshots and
E1/E4 measurements from the [implementation handoff](2026-09-11-S1.md) remain
the evidence for the unchanged application; the manual browser check was not
repeated for this checker-only change.

Root Biome passes with its existing two warnings and one informational diagnostic.
The only remaining hygiene conditions are:

1. `docs-parity: docs/architecture-managed-prefill-reuse.md must name its visual blueprint`
2. `docs-parity: docs/architecture-managed-prefill-reuse.md must have exactly one blueprint source mapping, found 0`

The unrelated document was not edited. The sprint explicitly accepts this
baseline during app work, so S1 can close. This is not a claim that root lint,
the full root CI pipeline, or release acceptance passes.

The sprint's current S1 status is `done`. The original in-progress ledger row
and root-edit request are preserved; new rows record approval, application, and
resolution. The class B table records the narrow operator-authorized exception,
and the app README links this closeout.

**Next slice: S2 — Trace explorer**, covering run pagination, full run detail,
and live trace tails. Its status remains `todo`; this session stops after S1.
All existing application changes remain under `apps/clio-coder-web/`. Across S1,
the root changes are the allowed manifest/lockfile edits and the now-approved
single checker line. Nothing was committed or pushed, and the branch did not change.
