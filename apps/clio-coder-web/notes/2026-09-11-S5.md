# S5 — unified shell, rich content, browser acceptance

Started from `9a3f3add28299bec6925f3bb26b5696b4211b6c5` on `v048`. The root source seams still match reviewed `1e1162f6`; intervening commits are the completed app slices and S1's approved manifest/checker changes. The operator authorized all S slices and atomic local commits, with no push.

Implemented shared navigation, responsive native-dialog navigation, route focus, a skip link, persisted light/dark themes, local fonts, the overview, and bounded dismissible Problem notifications with code and instance reference. Future routes remain visibly unavailable until their slices implement them. `DESIGN.md` records the actual rules.

Ported the Workbench Markdown lexer and React renderer, lazy Prism grammars and strict Mermaid renderer. Raw HTML remains text and chat links permit only HTTP, HTTPS and mailto. Mermaid SVG is sanitized with DOMPurify, parsed and imported as DOM; there is no `dangerouslySetInnerHTML`. Streaming and source limits remain. After mounting and loading local fonts, diagrams fit their actual SVG geometry; the browser test caught and fixed a clipped final node on mobile. Keyboard-focusable code viewports provide horizontal scrolling without widening the page. One narrow Biome accessibility suppression documents that need; no rule is disabled globally.

The application verify includes the automated browser smoke from this slice onward. It serves the real Hono app, uses scratch workers and a supervised deterministic ACP child, and cleans up its own browser, server, children and state. Chrome requests outside the loopback origin are refused and reported. Request cancellation is ignored only for intentionally closed SSE streams. Each width also covers approvals, cancellation, safe session controls, validation problems, and both themes.

Validation: `TMPDIR=/var/tmp/clio-web-verification pnpm --filter @iowarp/clio-coder-web verify` passes both TypeScript programs, Biome, 55 tests (including 13 ported Markdown scenarios, boundaries and OpenAPI), production build and browser smoke. Chrome 152.0.7977.82 checks 40 page/theme/width combinations at 1600, 1050 and 390 pixels. No page overflow, script errors, failed requests or unexpected HTTP errors. Three deliberate workspace validation requests return 422 to exercise Problem toasts. Keyboard navigation, dialog Escape/focus return, code scrolling, highlighting, sanitized diagrams and diagram bounds are asserted. Root typecheck passes. Root lint passes Biome with the existing two warnings/one info, then fails only the two documented docs-parity conditions for the unrelated untracked architecture document. This is not a whole-tree or release pass.

The build has 135 assets, 4,231,830 bytes total. JavaScript totals 3,942,842 bytes (1,123,085 bytes when each file is gzip-compressed independently using Python's defaults). The main entry is 516,540 bytes, 156,064 gzip; CSS is 23,247 bytes, 6,009 gzip. Lazy Mermaid chunks are included in the total. Vite's existing 500 kB chunk warning is retained: the entry and largest Mermaid chunk exceed it. No threshold was raised. S10 measures the actual package delta.

Added nine exact dependencies, all reusing versions already in the Workbench lock: Atkinson Hyperlegible Next 5.3.0, Newsreader 5.3.0, Commit Mono 5.3.0, marked 18.0.11, Prism 1.30.0, Mermaid 11.17.2, DOMPurify 3.4.14, playwright-core 1.62.1, and @axe-core/playwright 4.13.0. Frozen install passes. The only root edit in S5 is the 27-line app-importer addition to `pnpm-lock.yaml`; there are no unrelated upgrades.

Retained screenshots: [overview](2026-09-11-S5-home.png), [conversation](2026-09-11-S5-conversation.png), [mobile](2026-09-11-S5-mobile.png), [dark theme](2026-09-11-S5-dark.png). Full ephemeral logs and browser reports are in `/var/tmp/clio-web-verification/S5-closeout.log` and the ignored `dist/smoke/` directory.

Next: S6, the documentation tree, contained page/blueprint routes, link rewriting and search. No R-slice integration or push is authorized.
