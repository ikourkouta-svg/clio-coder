# S7a — effective settings and customization inspection

Started from `1c5dd3f6` on `v048`. S6 changed only app files; all root seams still match reviewed `1e1162f6`. S7a adds no dependencies and makes no root edits.

Implemented workspace-scoped, read-only Settings and Why pages. Settings shows the effective value and origin of every canonical leaf, a filter, layer presence/paths, and source issues. Why groups the customization entries by the categories returned by Clio, with source paths, scope, precedence, trust, reload behavior, hashes, context cost and numeric/boolean facts. Workspace selection is shared and carried in the URL. No write route is exposed.

`readLayeredSettings(cwd)` and `buildCustomizationGraph(cwd)` run in the reads worker. Both adapters load lazily; the graph has a 15-second deadline covering module load and execution. The graph's heavy transitive imports include context, extension snapshots, middleware hook sources, prompts, resources, safety and memory; that accepted seam cost stays off the HTTP process. The boundary test explicitly names the two root imports. The already-exported CLI graph builder is used without changing its exports.

Public projections live next to the worker adapters so sensitive source data is removed before RPC. This is a layout adjustment from the proposed service-only projection: HTTP services validate the closed public schemas. Settings masks credential/auth/header branches, environment maps and executable commands/argument vectors, strips userinfo/query/fragment from URLs, and bounds displayed strings. Private branches appear as a redacted parent row. Graph output omits raw settings values, string/object detail payloads, hook argv and raw issue messages; the companion Settings page provides the complete redacted settings view. Inspection issues are counted per category with safe messages.

Leaf origins use the canonical reader's source map. A partial object layer does not claim built-in defaults for absent children; arrays replace wholesale, so their origin applies to their contained leaves. Tests pin both cases. The reader supplies four file/default layers here; the schema admits `cli`, but the web inspection does not invent a CLI override layer. Session-specific safe settings remain in their ACP session controls.

Validation: `TMPDIR=/var/tmp/clio-web-verification pnpm --filter @iowarp/clio-coder-web verify` passes both TypeScript programs, Biome, 60 tests, build and the 61-check Chrome smoke. The fixtures call `initializeClioHome`, seed the real `credentials.yaml` through `openAuthStorage().setApiKey`, write and explicitly trust scratch project/local layers and a command hook, and set an environment API key in the reads worker. Every canonical settings leaf is represented or covered by an explicitly redacted parent; graph categories match the canonical builder. Credential values, configured/process environment values, header content and executable arguments never appear in either response. Credential, settings and hook files remain byte-identical after inspection. POST is refused on both read routes.

The real 15-second deadline test leaves the graph worker occupied past its deadline, verifies the HTTP metadata route remains responsive, receives `503 unavailable`, and then successfully reads the toolchain after the late graph result. Duration assertions use a monotonic clock. Synchronous work is not falsely reported as cancelled.

Chrome 152.0.7977.82 checks Settings and Why in both themes at 1600, 1050 and 390 pixels, in addition to the existing application flows. Zero Axe violations of any severity, overflow, script errors, failed requests or unexpected HTTP errors; the three deliberate workspace 422 responses remain. Retained screenshots: [Settings desktop](2026-09-11-S7a-settings.png), [Settings mobile](2026-09-11-S7a-mobile.png), [Why](2026-09-11-S7a-why.png). Logs are `/var/tmp/clio-web-verification/S7a-*`; detailed browser reports are in ignored `dist/smoke/`.

Root typecheck passes. Root lint passes Biome with existing warnings/info and fails only the two documented baseline docs-parity conditions. Vite's existing 500 kB chunk warning remains; no threshold was raised. No release pass is claimed.

Next: S7b, the fixed-argv CLI runner, target listing, probes, use and removal. Then S8a–S8e, S9 and S10 remain authorized; no push or R-slice work.
