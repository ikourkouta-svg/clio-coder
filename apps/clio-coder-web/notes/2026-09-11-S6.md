# S6 — package documentation and blueprints

Started from `2cc31787` on `v048`; root source seams still match the reviewed commit. S5 introduced only its allowed app-importer lockfile entries. S6 adds no dependencies and changes no root files.

The Docs page has a compact navigation tree grouped from the Markdown tables in `docs/README.md`, a complete scanned inventory, full page rendering, local heading anchors and search. Selecting a search result dismisses the results so the chosen guide is immediately useful. Markdown links resolve to app routes; source-checkout blueprint links resolve to `/docs-html/`. References outside the documentation that exist in the package become explicit public repository links. Unavailable destinations remain text with a visible disclosure explaining the unavailable references; the app does not read arbitrary local paths.

The reads worker hosts `DocsAdapter`, importing only the existing `resolvePackageRoot` seam. It builds the search/navigation index once per worker lifetime, independently of cwd or Git tracking. Limits: 1 MiB per Markdown page, 16 MiB / 10,000 documents per index, 200-character queries and 30 search results, 8 MiB per static blueprint asset. Page reads recheck realpath containment, including after a file is replaced with a symlink. Restarting the server refreshes the indexed corpus.

Blueprint routes preserve GET/HEAD, content length and MIME types, no-store, 405 with Allow, case-insensitive topic resolution, and synthesized menus. They reject malformed encoding, null bytes, dot segments, backslashes and symlink escapes. A guard checks Node's raw request target before route dispatch, because URL normalization otherwise removes encoded dot segments. Static failures use the shared Problem JSON contract. An absent blueprint directory leaves the packaged Markdown browser usable; it does not stop the app.

Blueprint HTML carries `sandbox allow-scripts` without `allow-same-origin`, with local scripts/styles and no network connections. The retained browser check opens the real trace blueprint, waits for its shared JavaScript to create copy controls, and proves that application session storage is inaccessible. It receives no launch token. The existing server shutdown/signal and owned-child cleanup tests remain in the full suite; S6 does not create another listener or signal lifecycle.

Validation: `TMPDIR=/var/tmp/clio-web-verification pnpm --filter @iowarp/clio-coder-web verify` passes both TypeScript programs, Biome, all 58 tests, the production client build, and 49 Chrome/Axe page/theme/width checks. The document walk discovers 60 Markdown pages at test time (including untracked pages), renders every one, checks 279 live internal links and serves all 59 referenced blueprints. Search for `trace` ranks `architecture/trace-store.md` in the first three results. Containment tests use actual raw HTTP requests, including traversal toward an otherwise valid API route. OpenAPI is regenerated and checked for drift.

The only unavailable source references are three absolute paths outside the repository in the unrelated untracked `docs/architecture-managed-prefill-reuse.md`. They remain visibly unavailable in the rendered draft. That file is unchanged (SHA-256 `18a96163685f11b5e244c29c6c25d2bdd7c234b47d52accecd6e1c9514082e56`). No documentation source was rewritten to satisfy acceptance.

Chrome 152.0.7977.82 checks Docs map, a searched guide and dark theme at 1600, 1050 and 390 pixels in addition to the prior app flows. No Axe violations of any severity, horizontal page overflow, script errors, failed requests, or unexpected HTTP errors. The three deliberate 422 workspace requests continue to exercise Problem toasts. [Desktop](2026-09-11-S6-desktop.png) and [mobile](2026-09-11-S6-mobile.png) screenshots are retained. Detailed reports remain in ignored `dist/smoke/`; verification logs are under `/var/tmp/clio-web-verification/S6-*`.

Root typecheck passes. Root lint passes Biome with the existing warnings/info, then fails only the two documented baseline docs-parity conditions. Vite still reports its 500 kB chunk-size warning; the budget is unchanged. This is app-slice acceptance, not a whole-tree release pass.

Next: S7a, read-only effective settings with origin layers and the customization graph. `src/cli/docs.ts` retirement remains R3 and is outside this authorization.
